# PersEUD (jMetal + ZMQ + Distributed Gradient Descent)

## Running the router

```
cd routers
./router.py
```

## Running the multicore worker

Compile with:

```
cd workers/multicore
. env.sh
make
```
And then run with:
```
./run.sh
```

